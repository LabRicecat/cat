#ifndef CAT_COMPOSER_LINENUMBERS_HPP
#define CAT_COMPOSER_LINENUMBERS_HPP

#include "../base/buffer.hpp"

namespace cat {

    Composer::return_type line_numbers(const Composer::param_type& param, Buffer* buffer);

} // namespace cat

#endif
